apiVersion: v1
kind: Service
metadata:
  name: eventfindr-be
  namespace: app
  labels:
    app: eventfindr-be
spec:
  selector:
    app: eventfindr-be
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP

