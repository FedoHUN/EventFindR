apiVersion: v1
kind: Service
metadata:
  name: eventfindr-fe
  namespace: app
  labels:
    app: eventfindr-fe
spec:
  selector:
    app: eventfindr-fe
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP

